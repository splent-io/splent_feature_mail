services:

  splent_feature_mail_mailhog:
    image: mailhog/mailhog
    ports:
      - "${MAILHOG_PORT_ONE}:8025"
      - "${MAILHOG_PORT_TWO}:1025"
    networks:
      - splent_network

networks:
  splent_network:
    external: true